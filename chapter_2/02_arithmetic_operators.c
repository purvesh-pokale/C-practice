#include <stdio.h>

int main()
 {
    int a=4;
    int b=3;
    int c = a+b;
    
    printf("the valeu of a is %d and value of b is %d and sum is %d \n", a , b , c);

    // modulus operator is used to get the remainder

    printf("The remainder when a is divided by b is: %d" , a%b);

    return 0;
}